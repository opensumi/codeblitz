import { ODPS } from 'bravo-parser';
/**
 * worker url
 */
export const EXT_WORKER_HOST = __WORKER_HOST__;

/**
 * webview endpoint
 */
export const WEBVIEW_ENDPOINT = __WEBVIEW_ENDPOINT__;

/**
 * 内网 webview endpoint，http 地址，开发时使用此地址，避免 https overload http 错误
 */
export const WEBVIEW_ENDPOINT_INTERNAL = __WEBVIEW_ENDPOINT_INTERNAL__;

/**
 * webview 脚本 url
 */
export const WEBVIEW_SCRIPT = __WEBVIEW_SCRIPT__;

/**
 * alex version
 */
export const VERSION = __VERSION__;


export const ODPS_WORKER = __ODPS_WORKER__;
